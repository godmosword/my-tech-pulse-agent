/**
 * Fallback right-rail content used by any route that doesn't define its own
 * `@rail/page.tsx`. Empty for now — routes can opt in incrementally.
 */
export default function DefaultRail() {
  return null;
}
